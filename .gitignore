# Obsidian — user-specific config
docs/.obsidian/

# Node / env
node_modules/
.env
.env.*
!.env.example

# Build output
dist/
build/
dev-dist/
apps/api/src/generated/

# TypeScript
*.tsbuildinfo

# Test output
playwright-report/
test-results/

# Vite
.vite/

# TanStack Router — auto-generated, regenerated by `vite dev`/`vite build`
# Committed initially so the app works on first clone; future updates are ignored.
**/routeTree.gen.ts

# Worktrees
.worktrees/
.claude/worktrees/

# Development plans — transient scratch written by /plan-build, never part of the
# second brain (docs/) or git history. Persist locally across build sessions.
plans/

# OS
.DS_Store

# Design folder
design-system/

# VSCode folder
.vscode/