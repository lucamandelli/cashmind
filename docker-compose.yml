services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: cashmind
      POSTGRES_PASSWORD: cashmind
      POSTGRES_DB: cashmind
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cashmind"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
